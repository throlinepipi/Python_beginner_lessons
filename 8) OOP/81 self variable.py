# default variable pointing to the current object.
# by using it, instance variables and instance methods can be accessed.

# inside the constructor, the first parameter should be self.
# inside instance methods, the first parameter should be self.
